<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CustomLayout08012024" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CustomLayout08012024" parent="Base.Theme.CustomLayout08012024" />

    <!-- common -->
    <declare-styleable name="ProductItemView">
        <attr name="title" format="string" />
        <attr name="subTitle" format="string" />
        <attr name="item_icon" format="reference" />
        <attr name="item_background_icon" format="reference" />
        <attr name="showTrailing" format="boolean"/>
    </declare-styleable>

    <style name="MatchParentStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="RatingBarStyle1" parent="Widget.AppCompat.RatingBar">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="background">@color/black</item>
        <item name="android:numStars">10</item>
    </style>

    <style name="RatingBarStyle2" parent="RatingBarStyle1">
        <item name="android:numStars">2</item>
    </style>
</resources>